export interface AuthorCreateDto {
  firstName: string;
  lastName: string;
}

export interface AuthorReturnDto {
  id: number
  firstName: string;
  lastName: string;
}

export interface AuthorUpdateDto {
  firstName: string;
  lastName: string;
}