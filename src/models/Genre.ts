export interface GenreCreateDto {
  name: string;
  description: string;
}

export interface GenreReturnDto {
  id: number;
  name: string;
  description: string;
}

export interface GenreUpdateDto {
  name: string;
  description: string;
}